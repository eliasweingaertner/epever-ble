{
  "config": {
    "step": {
      "user": {
        "title": "EPEver BLE Charge Controller",
        "description": "Connect to an EPEver Tracer charge controller via Bluetooth Low Energy.",
        "data": {
          "mac": "MAC Address",
          "scan_interval": "Update interval (seconds)"
        },
        "data_description": {
          "mac": "Bluetooth MAC address of the charge controller (e.g. AA:BB:CC:DD:EE:FF). Find it with `bluetoothctl scan on`.",
          "scan_interval": "How often to poll the controller. Minimum 10 seconds."
        }
      }
    },
    "error": {
      "invalid_mac": "Invalid MAC address format. Use XX:XX:XX:XX:XX:XX."
    },
    "abort": {
      "already_configured": "This device is already configured."
    }
  }
}
